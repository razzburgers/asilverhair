A SILVER HAIR OF WISDOM — v1.1 ARCHIVE EXPANSION

Built from the existing GitHub Pages package.

WHAT CHANGED
- Archive expanded from 10 to 50 complete entries.
- Entries #001–#010 preserved in substance.
- New #011–#050 broaden relationships, dating, boundaries, friendship,
  self-worth, aging, solitude, work, family, regret, and starting over.
- Every entry rebuilt with canonical metadata, Open Graph/Twitter metadata,
  BlogPosting structured data, topic labels, older/newer navigation,
  and contextual related-reading links.
- index.html now contains all 50 entries and remains searchable.
- sitemap.xml contains homepage, About page, and all 50 entries.
- rss.xml contains the latest 20 entries.
- robots.txt points to the sitemap.

DEPLOY
Upload/replace the repository contents at the root of the existing asilverhair
GitHub repository and commit to main. GitHub Pages is already configured for
main / (root).

NOTE
Entries #011–#050 are dated sequentially from Aug 11 through Sep 19, 2026.
If you prefer scheduled release instead of publishing all of them immediately,
do not deploy this package as-is; use a publishing-queue build next.
